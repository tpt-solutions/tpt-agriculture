# TPT Agriculture — Redevelopment Checklist
> Tauri 2 Desktop + PWA | Offline-first | Open Source Apache | No cloud required

---

## Phase 0 — Scaffold
- [ ] Add `apps/desktop/` directory
- [ ] Init Tauri 2 + Vite + React inside `apps/desktop/`
- [ ] Add `apps/desktop` to pnpm workspace and Turborepo pipeline
- [ ] Confirm `pnpm tauri dev` opens a working Tauri window
- [ ] Add `vite-plugin-pwa` to Vite config
- [ ] Confirm PWA manifest + service worker generated in build output

## Phase 1 — Database Layer
- [ ] Remove `packages/core/prisma/` folder
- [ ] Add Drizzle ORM + Drizzle Kit to `packages/core`
- [ ] Write Drizzle schema: `identity.ts` (users, sessions)
- [ ] Write Drizzle schema: `farm.ts` (farms, farm_users)
- [ ] Write Drizzle schema: `horticulture.ts` (all horticulture tables)
- [ ] Write Drizzle schema: `livestock.ts` (all livestock tables)
- [ ] Write `apps/desktop/src/db/adapter-tauri.ts` (plugin-sql SQLite)
- [ ] Write `apps/desktop/src/db/adapter-web.ts` (wa-sqlite + OPFS)
- [ ] Write `apps/desktop/src/db/index.ts` (`getDb()` with platform detection)
- [ ] Run `drizzle-kit generate` → confirm migration SQL files created
- [ ] Test: DB initialises on first launch in Tauri window
- [ ] Test: DB initialises in browser PWA (Chrome/Firefox with OPFS)

## Phase 2 — Local Auth
- [ ] Create `apps/desktop/src/auth/` module
- [ ] Build "Create Farm" setup wizard (farm name + owner account)
- [ ] Build login screen (email + password, bcrypt verify)
- [ ] Implement session context (React context + `useSession` hook)
- [ ] Add route guard: redirect to `/login` if no active session
- [ ] Test: create farm → restart app → login works correctly

## Phase 3 — App Shell
- [ ] Set up React Router v7 with all main routes
- [ ] Port sidebar + layout from `packages/ui/` components
- [ ] Build dashboard with module cards
- [ ] Build Settings → Users page (OWNER can add/edit/remove users)
- [ ] Build Settings → Farm (edit farm name, view farm ID)
- [ ] Test: all routes navigate correctly with correct layout

## Phase 4 — Module Services
- [ ] Update `packages/modules/field-management/src/service.ts` → Drizzle
- [ ] Update `packages/modules/crop-planning/src/service.ts` → Drizzle
- [ ] Update `packages/modules/harvest-tracking/src/service.ts` → Drizzle
- [ ] Update `packages/modules/pest-spray-log/src/service.ts` → Drizzle
- [ ] Update `packages/modules/viticulture/src/service.ts` → Drizzle
- [ ] Update `packages/modules/orchard/src/service.ts` → Drizzle
- [ ] Update `packages/modules/vegetables/src/service.ts` → Drizzle
- [ ] Update `packages/modules/microgreens/src/service.ts` → Drizzle
- [ ] Update `packages/modules/protected-cropping/src/service.ts` → Drizzle
- [ ] Update `packages/modules/aquaponics/src/service.ts` → Drizzle
- [ ] Update `packages/modules/cattle-dairy/src/service.ts` → Drizzle
- [ ] Update `packages/modules/cattle-beef/src/service.ts` → Drizzle
- [ ] Update `packages/modules/sheep/src/service.ts` → Drizzle
- [ ] Update `packages/modules/goats/src/service.ts` → Drizzle
- [ ] Update `packages/modules/deer/src/service.ts` → Drizzle
- [ ] Update `packages/modules/pigs/src/service.ts` → Drizzle
- [ ] Update `packages/modules/poultry/src/service.ts` → Drizzle
- [ ] Update `packages/modules/bees/src/service.ts` → Drizzle
- [ ] Update `packages/modules/pasture/src/service.ts` → Drizzle
- [ ] Wire each module's service into React Query hooks in app
- [ ] Test CRUD for at least one horticulture + one livestock module end-to-end

## Phase 5 — Tauri Build
- [ ] Configure `tauri.conf.json`: app name, identifier, icons, window size
- [ ] Add app icons (1024x1024 PNG → Tauri generates all sizes)
- [ ] Run `pnpm tauri build` on Windows → confirm `.exe` installer produced
- [ ] Run `pnpm tauri build` on Mac → confirm `.dmg` produced
- [ ] Test: install from `.exe`, launch, create farm, add record, reinstall → data persists

## Phase 6 — PWA Polish
- [ ] Tune `vite-plugin-pwa`: precache shell, all-local data (no network fetches)
- [ ] Test: install PWA in Chrome → go offline → confirm all navigation works
- [ ] Test: add record offline → reload → record persists (OPFS SQLite)
- [ ] Add install prompt banner (show when `beforeinstallprompt` fires)
- [ ] Run Lighthouse PWA audit → achieve 100 PWA score

## Phase 7 — Encrypted Backup (optional)
> Integrates with backup.tptsolutions.co.nz. Zero-knowledge: server never sees plaintext.

- [ ] Add `@tauri-apps/plugin-stronghold` for OS keychain storage
- [ ] Generate 256-bit key on farm setup using Argon2id
- [ ] Encode key as 24-word BIP39 recovery phrase
- [ ] Build recovery phrase screen with **"Email to myself"** button (opens mailto: link, works offline)
- [ ] Build recovery phrase screen with **"Copy to clipboard"** button
- [ ] Add mandatory "I have saved my recovery phrase" checkbox — setup blocks until ticked
- [ ] Store key in Tauri Stronghold (desktop) / encrypted IndexedDB (PWA)
- [ ] Add Settings → Backup → "View recovery phrase" (OWNER only, requires password re-entry)
- [ ] Implement: SQLite export → AES-256-GCM encrypt → upload to backup.tptsolutions.co.nz
- [ ] Implement: download backup list → select → enter recovery phrase → decrypt → import
- [ ] Test: backup → fresh install → restore with phrase → data matches

## Cleanup
- [ ] Remove `apps/web/` (entire Next.js app)
- [ ] Remove Supabase, Stripe, Prisma, next-pwa from all `package.json` files
- [ ] Remove `vercel.json`
- [ ] Update root README with new setup instructions
- [ ] Confirm `pnpm typecheck` passes with zero errors
